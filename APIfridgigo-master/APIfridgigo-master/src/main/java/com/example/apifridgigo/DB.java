package com.example.apifridgigo;

public class DB {
    create database DB;
    create table DB.rezepte(
        id int NOT NULL AUTO_INCREMENT primary key,
        rezept_id integer (500) NOT NULL,
        name varchar(50) NOT NULL,
        enname varchar (50) NOT NULL,
        portionen varchar (50) NOT NULL,
        zeit int NOT NULL );
    )
    
}
